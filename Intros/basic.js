// declaring a variable 

let variable = "Data";
var variableName = "Data"; // muiltlpe assignment are allowed

// when you want an immutable variable 
const immutable = "Data";

// Data types
let strings = "strings";

var numbers = 405833.284; //this includes both int and float

const boolean = true;//or false

// Logic and Comparison
/*
basic - +, -, *, /, **(expotential), **1/2 (square root), %, --, ++, postfix (x++), prefix (++x)
comp - >, <, >=, <=, ==, ===
*/
